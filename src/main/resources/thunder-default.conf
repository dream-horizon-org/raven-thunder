server.host = "0.0.0.0"
server.port = 8080
server.instances = 1
server.compressionSupported = true
server.idleTimeout = 60

aerospike.hosts = "aerospike:3000"
aerospike.namespace = "thunder"
aerospike.port = 3000
aerospike.socket-timeout = 5000
aerospike.total-timeout = 10000
aerospike.max-connections = 100

